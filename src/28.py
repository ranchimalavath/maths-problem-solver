def calculate_sum(n):
    """
    Calculate the sum of all natural numbers up to n.
    
    Args:
    - n: An integer representing the upper limit (inclusive).
    
    Returns:
    - The sum of all natural numbers from 1 to n.
    """
    return sum(range(1, n + 1))

calculate_sum(5)  # Example usage
